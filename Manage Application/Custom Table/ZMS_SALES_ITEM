@EndUserText.label : 'Sales Order Item'
@AbapCatalog.enhancement.category : #NOT_EXTENSIBLE
@AbapCatalog.tableCategory : #TRANSPARENT
@AbapCatalog.deliveryClass : #A
@AbapCatalog.dataMaintenance : #RESTRICTED
define table zms_sales_item {

  key client : abap.clnt not null;
  key vbeln  : vbeln_va not null;
  key posnr  : posnr_va not null;
  matnr      : matnr;
  arktx      : arktx;
  comment_f  : arktx;
  @Semantics.quantity.unitOfMeasure : 'zms_sales_item.vrkme'
  kwmeng     : abap.quan(15,3);
  vrkme      : vrkme;
  @Semantics.amount.currencyCode : 'zms_sales_item.waerk'
  netpr      : abap.curr(15,2);
  @Semantics.amount.currencyCode : 'zms_sales_item.waerk'
  netwr      : abap.curr(15,2);
  waerk      : waerk;

}